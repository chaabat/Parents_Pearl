# MongoDB Production Configuration
spring.data.mongodb.uri=mongodb://localhost:27017/parentspearl
spring.data.mongodb.database=parentspearl_prod
spring.data.mongodb.auto-index-creation=true

# Logging
logging.level.org.springframework.data.mongodb=INFO

# Server Configuration
server.port=8080

# Remove H2 and JPA configurations since we're using MongoDB

